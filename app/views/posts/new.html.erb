<%= form_for @post do |f| %>

  <input type="hidden" value="<%= form_authenticity_token %>" name="authenticity_token">
  <%= f.date_field :date %>
  <%= f.text_area :rationale %>
  <%= f.submit 'Save' %>

<% end %>